import { render, screen } from "../../../utils/test-utils";
import "@testing-library/jest-dom";

// it shows the targetBox on click

// it shows markers properly

//it shows the map

// it shows the right characters for the dropdown

// it blocks clicks if game is over
